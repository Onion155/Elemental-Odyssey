using UnityEngine;

public class PlayerSpawnControler : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        // find player 
        // set their pos to the spawnpoint (this should be a public function that can be called by other scripts)
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
